#include "../cub3d.h"

int	parse_map(char *line, t_data *data, int fd, int fd_len)
{
	// parse first line
	// then get_line again for dimensions (+ invalid char check)
	// restart with read(fd_len) to parse map properly
}

int	detect_map(char *line)
{

}

void check_elements_presence(data)
{

	clean(data, "Element missing\n", 1);
}
